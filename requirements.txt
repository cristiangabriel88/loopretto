# Lockfile - pinned transitive set for reproducible installs.
# Top-level (direct) dependencies live in requirements.in (the manifest).
# Regenerate after changing the manifest:
#   uv pip compile requirements.in -o requirements.txt
# Then keep yt-dlp as a range (see below / requirements.in, §8.3).
blinker==1.9.0
click==8.2.1
colorama==0.4.6
Deprecated==1.2.18
Flask==3.1.3
Flask-Limiter==3.12
imageio-ffmpeg==0.5.1
itsdangerous==2.2.0
Jinja2==3.1.6
limits==5.4.0
markdown-it-py==3.0.0
MarkupSafe==3.0.2
mdurl==0.1.2
ordered-set==4.1.0
packaging==25.0
Pygments==2.20.0
rich==13.9.4
typing_extensions==4.14.0
Werkzeug==3.1.8
wrapt==1.17.2
# Intentionally a range, not a pin - YouTube breaks extractors often, so a
# stale yt-dlp silently breaks downloads (§8.3). A fresh install picks the
# latest 2026.x; Dependabot keeps it moving (see .github/dependabot.yml).
yt-dlp>=2026.02.21,<2027
